# Naming Conventions
Code: camelCase (getUserById). Components: PascalCase (UserProfile).
API endpoints: kebab-case plural (/api/user-profiles).
Database: snake_case (first_name, user_id).
Git: branch feature/TASK-001-desc, commit type(scope): desc
