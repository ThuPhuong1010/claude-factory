# Error Handling
Every async → try-catch. No empty catches.
User-facing: friendly message. Dev: console.error with context.
API: consistent { success, error: { code, message } } format.
React ErrorBoundary per page.
