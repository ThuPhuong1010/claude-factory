When editing .tsx component files with UI:
- Use Ant Design components. No custom replacements.
- Every page needs: loading state (Skeleton), empty state (Empty), error state (Result).
- Responsive: use Ant Grid Row/Col with breakpoints.
- Colors: use ConfigProvider tokens only, no hardcoded hex.
- Read .claude/skills/ant-design/SKILL.md for component patterns.
