When editing React component files (*.tsx):
- Use Ant Design components. Do not create custom replacements.
- 'use client' directive required when using hooks or event handlers
- Props interface: {ComponentName}Props
- Export default for page components, named export for shared
